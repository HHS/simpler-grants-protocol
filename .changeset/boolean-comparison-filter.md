---
"@common-grants/core": minor
---

Add `BooleanComparisonFilter` to the core filter catalog (#895). `BooleanComparisonFilter`
compares a boolean value with `eq`/`neq`. It is added at protocol version 0.4.0 and emitted to
JSON Schema output.
