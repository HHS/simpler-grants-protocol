---
title: Date and time types
description: Date and time types used throughout the protocol.
sidebar:
  order: 3
isoDate:
  example:
    code: '"2025-01-01"'
  jsonSchema:
    file: 
      path: "website/public/schemas/yaml/isoDate.yaml"
  typeSpec:
    file:
      path: "specs/lib/core/types.tsp"
      startLine: 28
      endLine: 30
isoTime:
  example:
    code: '"17:00:00"'
  jsonSchema:
    file: 
      path: "website/public/schemas/yaml/isoTime.yaml"
  typeSpec:
    file:
      path: "specs/lib/core/types.tsp"
      startLine: 24
      endLine: 27
utcDateTime:
  example:
    code: '"2024-02-28T17:00:00Z"'
  jsonSchema:
    code: |
      $schema: https://json-schema.org/draft/2020-12/schema
      $id: utcDateTime.yaml
      type: string
      format: date-time
      description: A date and time with timezone in ISO 8601 format YYYY-MM-DDThh:mm:ssZ
offsetDateTime:
  example:
    code: '"2024-02-28T17:00:00+01:00"'
  jsonSchema:
    code: |
      $schema: https://json-schema.org/draft/2020-12/schema
      $id: offsetDateTime.yaml
      type: string
      format: date-time
      description: A date and time with timezone in ISO 8601 format YYYY-MM-DDThh:mm:ss±hh:mm
calendarYear:
  example:
    code: '"2025"'
  jsonSchema:
    file: 
      path: "website/public/schemas/yaml/calendarYear.yaml"
  typeSpec:
    file:
      path: "specs/lib/core/types.tsp"
      startLine: 32
      endLine: 35
---

import SchemaFormatTabs from "@/components/SchemaFormatTabs.astro";

## isoDate

A date on a calendar in ISO 8601 format YYYY-MM-DD.

<SchemaFormatTabs
  example={frontmatter.isoDate.example}
  jsonSchema={frontmatter.isoDate.jsonSchema}
  typeSpec={frontmatter.isoDate.typeSpec}
/>

## isoTime

A time on a clock, without a timezone, in ISO 8601 format HH:mm:ss.

:::caution
The `isoTime` type does not include timezone information. When representing a time in a specific timezone, use `utcDateTime` or `offsetDateTime` instead.
:::

<SchemaFormatTabs
  example={frontmatter.isoTime.example}
  jsonSchema={frontmatter.isoTime.jsonSchema}
  typeSpec={frontmatter.isoTime.typeSpec}
/>

## utcDateTime

A date and time with timezone in ISO 8601 format YYYY-MM-DDThh:mm:ssZ.

<SchemaFormatTabs
  example={frontmatter.utcDateTime.example}
  jsonSchema={frontmatter.utcDateTime.jsonSchema}
/>

## offsetDateTime

A date and time with timezone in ISO 8601 format YYYY-MM-DDThh:mm:ss±hh:mm.

<SchemaFormatTabs
  example={frontmatter.offsetDateTime.example}
  jsonSchema={frontmatter.offsetDateTime.jsonSchema}
/>

## calendarYear

A calendar year in the format YYYY.

<SchemaFormatTabs
  example={frontmatter.calendarYear.example}
  jsonSchema={frontmatter.calendarYear.jsonSchema}
  typeSpec={frontmatter.calendarYear.typeSpec}
/>
