---
title: String types
description: String types used throughout the protocol.
sidebar:
  order: 1
string:
  example:
    code: '"Hello, world!"'
  jsonSchema:
    code: |
      $schema: https://json-schema.org/draft/2020-12/schema
      $id: string.yaml
      type: string
      description: A sequence of characters
url:
  example:
    code: '"https://example.com/path"'
  jsonSchema:
    code: |
      $schema: https://json-schema.org/draft/2020-12/schema
      $id: url.yaml
      type: string
      format: uri
      description: A Uniform Resource Locator (URL)
uuid:
  example:
    code: '"30a12e5e-5940-4c08-921c-17a8960fcf4b"'
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/uuid.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/types.tsp"
      startLine: 28
      endLine: 31
  typescript:
    file:
      path: "lib/ts-sdk/src/schemas/zod/types.ts"
      startLine: 15
      endLine: 15
email:
  example:
    code: '"test@example.com"'
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/email.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/types.tsp"
      startLine: 33
      endLine: 37
employerTaxId:
  example:
    code: '"12-3456789"'
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/employerTaxId.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/types.tsp"
      startLine: 39
      endLine: 43
samUEI:
  example:
    code: '"12ABC34DEF56"'
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/samUEI.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/types.tsp"
      startLine: 45
      endLine: 49
duns:
  example:
    code: '"12-345-6789"'
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/duns.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/types.tsp"
      startLine: 51
      endLine: 56
---

import SchemaFormatTabs from "@/components/SchemaFormatTabs.astro";

## string

A sequence of characters.

<SchemaFormatTabs
  example={frontmatter.string.example}
  jsonSchema={frontmatter.string.jsonSchema}
/>

## url

A Uniform Resource Locator (URL).

<SchemaFormatTabs
  example={frontmatter.url.example}
  jsonSchema={frontmatter.url.jsonSchema}
/>

## uuid

A universally unique identifier.

<SchemaFormatTabs
  example={frontmatter.uuid.example}
  jsonSchema={frontmatter.uuid.jsonSchema}
  typeSpec={frontmatter.uuid.typeSpec}
  typescript={frontmatter.uuid.typescript}
/>

## email

An email address.

<SchemaFormatTabs
  example={frontmatter.email.example}
  jsonSchema={frontmatter.email.jsonSchema}
  typeSpec={frontmatter.email.typeSpec}
/>

## employerTaxId

An Employer Identification Number (EIN).

<SchemaFormatTabs
  example={frontmatter.employerTaxId.example}
  jsonSchema={frontmatter.employerTaxId.jsonSchema}
  typeSpec={frontmatter.employerTaxId.typeSpec}
/>

## samUEI

A Unique Entity Identifier (UEI).

<SchemaFormatTabs
  example={frontmatter.samUEI.example}
  jsonSchema={frontmatter.samUEI.jsonSchema}
  typeSpec={frontmatter.samUEI.typeSpec}
/>

## duns

A Data Universal Numbering System (DUNS) number.

<SchemaFormatTabs
  example={frontmatter.duns.example}
  jsonSchema={frontmatter.duns.jsonSchema}
  typeSpec={frontmatter.duns.typeSpec}
/>
