---
title: Money filters
description: Filters applied to monetary values.
moneyRangeFilter:
  example:
    code: |
      {
        "operator": "within",
        "value": {
          "min": {
            "amount": "1000",
            "currency": "USD"
          },
          "max": {
            "amount": "10000",
            "currency": "USD"
          }
        }
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/MoneyRangeFilter.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/filters/money.tsp"
      startLine: 24
      endLine: 39
  python:
    file:
      path: "templates/fast-api/src/common_grants/schemas/filters/money_filters.py"
      startLine: 42
      endLine: 60
moneyComparisonFilter:
  example:
    code: |
      {
        "operator": "eq",
        "value": {
          "amount": "1000",
          "currency": "USD"
        }
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/MoneyComparisonFilter.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/filters/money.tsp"
      startLine: 10
      endLine: 18
  python:
    file:
      path: "templates/fast-api/src/common_grants/schemas/filters/money_filters.py"
      startLine: 63
      endLine: 70
---

import SchemaFormatTabs from "@/components/SchemaFormatTabs.astro";

## MoneyRangeFilter

Filters by comparing a field to a range of monetary values.

### Table

##### Filter schema

| Property | Type                                                    | Description                               |
| -------- | ------------------------------------------------------- | ----------------------------------------- |
| operator | [RangeOperators](/protocol/filters/base#rangeoperators) | The operator to apply to the filter value |
| value    | [range object](#range-object)                           | The value to use for the filter operation |

##### Range object

| Property | Type                             | Description                               |
| -------- | -------------------------------- | ----------------------------------------- |
| min      | [Money](/protocol/fields/money/) | The minimum monetary value for this range |
| max      | [Money](/protocol/fields/money/) | The maximum monetary value for this range |

### Formats

<SchemaFormatTabs
  example={frontmatter.moneyRangeFilter.example}
  jsonSchema={frontmatter.moneyRangeFilter.jsonSchema}
  typeSpec={frontmatter.moneyRangeFilter.typeSpec}
  python={frontmatter.moneyRangeFilter.python}
/>

## MoneyComparisonFilter

Filters by comparing a field to a monetary value.

### Table

| Property | Type                                                              | Description                               |
| -------- | ----------------------------------------------------------------- | ----------------------------------------- |
| operator | [ComparisonOperators](/protocol/filters/base#comparisonoperators) | The operator to apply to the filter value |
| value    | [Money](/protocol/fields/money/)                                  | The value to use for the filter operation |

### Formats

<SchemaFormatTabs
  example={frontmatter.moneyComparisonFilter.example}
  jsonSchema={frontmatter.moneyComparisonFilter.jsonSchema}
  typeSpec={frontmatter.moneyComparisonFilter.typeSpec}
  python={frontmatter.moneyComparisonFilter.python}
/>
