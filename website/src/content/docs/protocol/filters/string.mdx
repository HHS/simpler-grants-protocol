---
title: String filters
description: Filters that can be applied to string or enum fields.
stringComparisonFilter:
  example:
    code: |
      {
        "operator": "eq",
        "value": "value"
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/StringComparisonFilter.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/filters/string.tsp"
      startLine: 9
      endLine: 17
stringArrayFilter:
  example:
    code: |
      {
        "operator": "eq",
        "value": ["value1", "value2"]
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/StringArrayFilter.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/filters/string.tsp"
      startLine: 23
      endLine: 31
---

import SchemaFormatTabs from "@/components/SchemaFormatTabs.astro";

## StringComparisonFilter

A filter that applies a comparison to a string value.

### Table

| Property | Type                                                      | Required | Description                               |
| -------- | --------------------------------------------------------- | -------- | ----------------------------------------- |
| operator | [StringOperators](/protocol/filters/base#stringoperators) | Yes      | The operator to apply to the filter value |
| value    | [string](/protocol/types/string#string)                   | Yes      | The value to use for the filter operation |

### Formats

<SchemaFormatTabs
  example={frontmatter.stringComparisonFilter.example}
  jsonSchema={frontmatter.stringComparisonFilter.jsonSchema}
  typeSpec={frontmatter.stringComparisonFilter.typeSpec}
/>

## StringArrayFilter

A filter that applies a comparison to an array of string values.

### Table

| Property | Type                                                                            | Required | Description                               |
| -------- | ------------------------------------------------------------------------------- | -------- | ----------------------------------------- |
| operator | [StringOperators](/protocol/filters/base#stringoperators)                       | Yes      | The operator to apply to the filter value |
| value    | [Array](/protocol/types/other#array)\<[string](/protocol/types/string#string)\> | Yes      | The value to use for the filter operation |

### Formats

<SchemaFormatTabs
  example={frontmatter.stringArrayFilter.example}
  jsonSchema={frontmatter.stringArrayFilter.jsonSchema}
  typeSpec={frontmatter.stringArrayFilter.typeSpec}
/>
