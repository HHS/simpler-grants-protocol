---
title: Application
description: Data model for an application to a funding opportunity
sidebar:
  order: 3
applicationBase:
  example:
    code: |
      {
        "id": "083b4567-e89d-42c8-a439-6c1234567890",
        "title": "Example Application",
        "competitionId": "083b4567-e89d-42c8-a439-6c1234567890",
        "formResponses": {
          // Form responses would be here
        },
        "status": {
          "value": "inProgress",
          "description": "Application is in progress."
        },
        "validationErrors": [],
        "submittedAt": null,
        "customFields": {
          "pctComplete": {
            "name": "Percentage Complete",
            "fieldType": "string",
            "value": "50%",
            "description": "Percentage of the application that has been completed"
          }
        }
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/ApplicationBase.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/models/application.tsp"
      startLine: 20
      endLine: 51
appRef:
  example:
    file:
      path: "website/public/schemas/yaml/AppRef.yaml"
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/AppRef.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/models/application.tsp"
      startLine: 3
      endLine: 18
appStatus:
  example:
    code: |
      {
        "value": "submitted",
        "description": "Application has been submitted."
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/AppStatus.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/models/application.tsp"
      startLine: 57
      endLine: 69
appStatusOptions:
  example:
    code: '"submitted"'
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/AppStatusOptions.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/models/application.tsp"
      startLine: 75
      endLine: 89
appFormResponse:
  example:
    code: |
      {
        "id": "083b4567-e89d-42c8-a439-6c1234567890",
        "form": {
          "id": "b7c1e2f4-8a3d-4e2a-9c5b-1f2e3d4c5b6a",
          "name": "Form A",
          "description": "Form A description"
        },
        "response": {
          "name": {
            "first": "John",
            "last": "Doe"
          },
          "email": "john.doe@example.com",
          "phone": "555-123-4567"
        },
        "status": {
          "value": "complete",
          "description": "The form response is complete"
        },
        "validationErrors": [],
        "applicationId": "083b4567-e89d-42c8-a439-6c1234567890",
        "createdAt": "2024-01-01T00:00:00Z",
        "lastModifiedAt": "2024-01-01T00:00:00Z"
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/AppFormResponse.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/models/application.tsp"
      startLine: 95
      endLine: 104
---

import SchemaFormatTabs from "@/components/SchemaFormatTabs.astro";
import SchemaTable from "@/components/SchemaTable.astro";
import SchemaChangelog from "@/components/SchemaChangelog.astro";

## ApplicationBase

Base model for an application to a funding opportunity.

### Table

<SchemaTable filePath={frontmatter.applicationBase.jsonSchema.file.path} />

### Formats

<SchemaFormatTabs
  example={frontmatter.applicationBase.example}
  jsonSchema={frontmatter.applicationBase.jsonSchema}
  typeSpec={frontmatter.applicationBase.typeSpec}
/>

### Changelog

<SchemaChangelog schema="ApplicationBase" />

## AppRef

A reference to an application, previewing the key fields a consumer needs to identify it and look it up or join it to another response. Appears wherever another record points to an application, such as the application an [award](/protocol/models/award) resulted from.

### Table

<SchemaTable filePath={frontmatter.appRef.jsonSchema.file.path} />

### Formats

<SchemaFormatTabs
  example={frontmatter.appRef.example}
  jsonSchema={frontmatter.appRef.jsonSchema}
  typeSpec={frontmatter.appRef.typeSpec}
/>

### Changelog

<SchemaChangelog schema="AppRef" />

## AppStatus

The status of an application.

### Table

<SchemaTable filePath={frontmatter.appStatus.jsonSchema.file.path} />

### Formats

<SchemaFormatTabs
  example={frontmatter.appStatus.example}
  jsonSchema={frontmatter.appStatus.jsonSchema}
  typeSpec={frontmatter.appStatus.typeSpec}
/>

### Changelog

<SchemaChangelog schema="AppStatus" />

## AppStatusOptions

The default set of values accepted for application status.

### Table

<SchemaTable filePath={frontmatter.appStatusOptions.jsonSchema.file.path} />

### Formats

<SchemaFormatTabs
  example={frontmatter.appStatusOptions.example}
  jsonSchema={frontmatter.appStatusOptions.jsonSchema}
  typeSpec={frontmatter.appStatusOptions.typeSpec}
/>

### Changelog

<SchemaChangelog schema="AppStatusOptions" />

## AppFormResponse

A form response associated with an application.

### Table

<SchemaTable filePath={frontmatter.appFormResponse.jsonSchema.file.path} />

### Formats

<SchemaFormatTabs
  example={frontmatter.appFormResponse.example}
  jsonSchema={frontmatter.appFormResponse.jsonSchema}
  typeSpec={frontmatter.appFormResponse.typeSpec}
/>

### Changelog

<SchemaChangelog schema="AppFormResponse" />
