---
title: Person
description: Data model for a person
---

import { Tabs, TabItem } from "@astrojs/starlight/components";

## Person

A person affiliated with an organization or grant application.

### Table

| Property    | Type                                                | Required | Description                  |
| ----------- | --------------------------------------------------- | -------- | ---------------------------- |
| name        | [Name](/protocol/fields/name)                       | Yes      | The person's full name       |
| title       | [string](/protocol/types/string#string)             | No       | The person's title           |
| dateOfBirth | [isoDate](/protocol/types/date#isodate)             | No       | The person's date of birth   |
| addresses   | [PersonAddressCollection](#personaddresscollection) | No       | The person's addresses       |
| phones      | [PersonPhoneCollection](#personphonecollection)     | No       | The person's phone numbers   |
| emails      | [PersonEmailCollection](#personemailcollection)     | No       | The person's email addresses |

### Formats

<Tabs>
  <TabItem label="Example">

A JSON example of this model is:

```json
{
  "name": {
    "prefix": "Dr.",
    "first": "Jane",
    "middle": "E.",
    "last": "Doe"
  },
  "title": "Chief Executive Officer",
  "dateOfBirth": "1990-01-01",
  "phones": {
    "primary": {
      "countryCode": "+1",
      "number": "123-456-7890"
    },
    "work": {
      "countryCode": "+1",
      "number": "123-456-7890"
    },
    "home": {
      "countryCode": "+1",
      "number": "123-456-7890"
    }
  },
  "fax": {
    "countryCode": "+1",
    "number": "123-456-7890"
  },
  "emails": {
    "primary": "john.doe@example.com",
    "work": "john.doe@work.com",
    "personal": "john.doe@gmail.com",
    "otherEmails": {
      "school": "john.doe@school.edu"
    }
  },
  "addresses": {
    "mailing": {
      "street1": "123 Main St",
      "city": "Anytown",
      "stateOrProvince": "CA",
      "country": "US",
      "postalCode": "12345"
    },
    "work": {
      "street1": "123 Main St",
      "city": "Anytown",
      "stateOrProvince": "CA",
      "country": "US",
      "postalCode": "12345"
    },
    "home": {
      "street1": "123 Main St",
      "city": "Anytown",
      "stateOrProvince": "CA",
      "country": "US",
      "postalCode": "12345"
    }
  }
}
```

  </TabItem>
  <TabItem label="JSON Schema">

The JSON Schema for this model is:

```yaml
$id: Person.yaml
type: object
properties:
  name:
    $ref: Name.yaml
    description: The person's full name, including all relevant components (first, middle, last, etc.).
  title:
    type: string
    description: The person's title, if applicable.
  dateOfBirth:
    $ref: isoDate.yaml
    description: The person's date of birth.
  addresses:
    $ref: PersonAddressCollection.yaml
    description: Collection of physical addresses associated with the person.
  phones:
    $ref: PersonPhoneCollection.yaml
    description: Collection of phone numbers associated with the person.
  emails:
    $ref: PersonEmailCollection.yaml
    description: Collection of email addresses associated with the person.
required:
  - name
description: A person affiliated with an organization or grant application.
```

  </TabItem>
  <TabItem label="TypeSpec">

The TypeSpec code for this model is:

```tsp
/** A person affiliated with an organization or grant application. */
model Person {
  /** The person's full name, including all relevant components (first, middle, last, etc.). */
  name: Fields.Name;

  /** The person's title, if applicable. */
  title?: string;

  /** The person's date of birth. */
  dateOfBirth?: Fields.isoDate;

  /** Collection of physical addresses associated with the person. */
  addresses?: PersonAddressCollection;

  /** Collection of phone numbers associated with the person. */
  phones?: PersonPhoneCollection;

  /** Collection of email addresses associated with the person. */
  emails?: PersonEmailCollection;
}
```

  </TabItem>
</Tabs>

## PersonAddressCollection

A collection of addresses for a person.

### Table

| Property       | Type                                                                          | Required | Description                                              |
| -------------- | ----------------------------------------------------------------------------- | -------- | -------------------------------------------------------- |
| primary        | [Address](/protocol/fields/address)                                           | Yes      | The person's mailing address                             |
| work           | [Address](/protocol/fields/address)                                           | No       | The person's work address                                |
| home           | [Address](/protocol/fields/address)                                           | No       | The person's home address                                |
| otherAddresses | [Record](/protocol/types/other#record)\<[Address](/protocol/fields/address)\> | No       | Additional addresses not covered by the standard fields. |

### Formats

<Tabs>
  <TabItem label="Example">

A JSON example of this model is:

```json
{
  "primary": {
    "street1": "123 Main St",
    "city": "Anytown",
    "stateOrProvince": "CA",
    "country": "US",
    "postalCode": "12345"
  },
  "work": {
    "street1": "123 Main St",
    "city": "Anytown",
    "stateOrProvince": "CA",
    "country": "US",
    "postalCode": "12345"
  },
  "home": {
    "street1": "123 Main St",
    "city": "Anytown",
    "stateOrProvince": "CA",
    "country": "US",
    "postalCode": "12345"
  },
  "otherAddresses": {
    "school": {
      "street1": "123 Main St",
      "city": "Anytown",
      "stateOrProvince": "CA",
      "country": "US",
      "postalCode": "12345"
    }
  }
}
```

  </TabItem>
  <TabItem label="JSON Schema">

The JSON Schema for this model is:

```yaml
$id: PersonAddressCollection.yaml
type: object
properties:
  primary:
    $ref: Address.yaml
    description: The person's primary mailing address.
  work:
    $ref: Address.yaml
    description: The person's work address.
  home:
    $ref: Address.yaml
    description: The person's home address.
  otherAddresses:
    type: object
    properties: {}
    unevaluatedProperties:
      $ref: Address.yaml
    description: Additional addresses not covered by the standard fields.
required:
  - primary
description: Collection of physical addresses associated with the person.
```

  </TabItem>
  <TabItem label="TypeSpec">

The TypeSpec code for this model is:

```tsp
/** A collection of addresses for a person. */
model PersonAddressCollection {
  /** The person's primary mailing address. */
  primary: Fields.Address;

  /** The person's work address. */
  work?: Fields.Address;

  /** The person's home address. */
  home?: Fields.Address;

  /** The person's fax number, if applicable. */
  fax?: Fields.Phone;

  /** Additional addresses not covered by the standard fields. */
  otherAddresses?: Record<Fields.Address>;
}
```

  </TabItem>
</Tabs>

## PersonEmailCollection

A collection of email addresses for a person.

### Table

| Property    | Type                                                                            | Required | Description                                                    |
| ----------- | ------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------- |
| primary     | [email](/protocol/types/string#email)                                           | Yes      | The person's primary email address                             |
| work        | [email](/protocol/types/string#email)                                           | No       | The person's work email address                                |
| home        | [email](/protocol/types/string#email)                                           | No       | The person's home email address                                |
| otherEmails | [Record](/protocol/types/other#record)\<[email](/protocol/types/string#email)\> | No       | Additional email addresses not covered by the standard fields. |

### Formats

<Tabs>
  <TabItem label="Example">

A JSON example of this model is:

```json
{
  "primary": "john.doe@example.com",
  "work": "john.doe@work.com",
  "home": "john.doe@gmail.com",
  "otherEmails": {
    "school": "john.doe@school.edu"
  }
}
```

  </TabItem>
  <TabItem label="JSON Schema">

The JSON Schema for this model is:

```yaml
$id: PersonEmailCollection.yaml
type: object
properties:
  primary:
    $ref: Email.yaml
    description: The person's primary email address.
  work:
    $ref: Email.yaml
    description: The person's work email address.
  home:
    $ref: Email.yaml
    description: The person's home email address.
  otherEmails:
    type: object
    properties: {}
    unevaluatedProperties:
      $ref: Email.yaml
    description: Additional email addresses not covered by the standard fields.
required:
  - primary
description: Collection of email addresses associated with the person.
```

  </TabItem>
  <TabItem label="TypeSpec">

The TypeSpec code for this model is:

```tsp
/** A collection of email addresses for a person. */
model PersonEmailCollection {
  /** The person's primary email address. */
  primary: Fields.email;

  /** The person's work email address. */
  work?: Fields.email;

  /** The person's personal email address. */
  personal?: Fields.email;

  /** Additional email addresses not covered by the standard fields. */
  otherEmails?: Record<Fields.email>;
}
```

  </TabItem>
</Tabs>

## PersonPhoneCollection

A collection of phone numbers for a person.

### Table

| Property    | Type                                                                      | Required | Description                                                  |
| ----------- | ------------------------------------------------------------------------- | -------- | ------------------------------------------------------------ |
| primary     | [Phone](/protocol/fields/phone)                                           | Yes      | The person's primary phone number                            |
| work        | [Phone](/protocol/fields/phone)                                           | No       | The person's work phone number                               |
| home        | [Phone](/protocol/fields/phone)                                           | No       | The person's home phone number                               |
| fax         | [Phone](/protocol/fields/phone)                                           | No       | The person's fax number, if applicable.                      |
| otherPhones | [Record](/protocol/types/other#record)\<[Phone](/protocol/fields/phone)\> | No       | Additional phone numbers not covered by the standard fields. |

### Formats

<Tabs>
  <TabItem label="Example">

A JSON example of this model is:

```json
{
  "primary": {
    "countryCode": "+1",
    "number": "123-456-7890",
    "isMobile": true
  },
  "work": {
    "countryCode": "+1",
    "number": "456-789-0123",
    "extension": "123",
    "isMobile": false
  },
  "home": {
    "countryCode": "+1",
    "number": "789-012-3456",
    "extension": "567",
    "isMobile": true
  },
  "otherPhones": {
    "school": {
      "countryCode": "+1",
      "number": "123-456-7890",
      "extension": "123",
      "isMobile": true
    }
  }
}
```

  </TabItem>
  <TabItem label="JSON Schema">

The JSON Schema for this model is:

```yaml
$id: PersonPhoneCollection.yaml
type: object
properties:
  primary:
    $ref: Phone.yaml
    description: The person's primary phone number.
  work:
    $ref: Phone.yaml
    description: The person's work phone number.
  home:
    $ref: Phone.yaml
    description: The person's home phone number.
  fax:
    $ref: Phone.yaml
    description: The person's fax number, if applicable.
  otherPhones:
    type: object
    properties: {}
    unevaluatedProperties:
      $ref: Phone.yaml
    description: Additional phone numbers not covered by the standard fields.
required:
  - primary
description: Collection of phone numbers associated with the person.
```

  </TabItem>
  <TabItem label="TypeSpec">

The TypeSpec code for this model is:

```tsp
/** A collection of phone numbers for a person. */
model PersonPhoneCollection {
  /** The person's primary phone number. */
  primary: Fields.Phone;

  /** The person's work phone number. */
  work?: Fields.Phone;

  /** The person's home phone number. */
  home?: Fields.Phone;

  /** The person's fax number, if applicable. */
  fax?: Fields.Phone;

  /** Additional phone numbers not covered by the standard fields. */
  otherPhones?: Record<Fields.Phone>;
}
```

  </TabItem>
</Tabs>
