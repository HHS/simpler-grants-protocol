---
title: CustomField
description: A model for defining custom fields that are specific to a given implementation
customField:
  example:
    code: |
      {
        "name": "programArea",
        "type": "string",
        "value": "Healthcare Innovation",
        "description": "Primary focus area of the grant program",
        "schema": "https://example.com/program-areas.json"
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/CustomField.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/custom-field.tsp"
      startLine: 46
      endLine: 62
  python:
    code: |
      from common_grants_sdk.schemas.fields import CustomField, CustomFieldType

      custom_field = CustomField(
          name="programArea",
          field_type=CustomFieldType.STRING,
          value="Healthcare Innovation",
          description="Primary focus area of the grant program",
          schema="https://example.com/program-areas.json"
      )
customFieldType:
  example:
    code: "string"
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/CustomFieldType.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/custom-field.tsp"
      startLine: 7
      endLine: 15
  python:
    file:
      path: "lib/python-sdk/common_grants_sdk/schemas/fields.py"
      startLine: 160
      endLine: 168
---

import SchemaFormatTabs from "@/components/SchemaFormatTabs.astro";

A model for defining custom fields on a record.

## CustomField

### Table

| Property    | Type                                    | Required | Description                                               |
| ----------- | --------------------------------------- | -------- | --------------------------------------------------------- |
| name        | [string](/protocol/types/string#string) | Yes      | Name of the custom field                                  |
| fieldType   | [CustomFieldType](#customfieldtype)     | Yes      | The JSON schema type to use when de-serializing the value |
| schema      | [url](/protocol/types/string#url)       | No       | Link to the full JSON schema for this custom field        |
| value       | any                                     | Yes      | Value of the custom field                                 |
| description | [string](/protocol/types/string#string) | No       | Description of the custom field's purpose                 |

### Formats

<SchemaFormatTabs
  example={frontmatter.customField.example}
  jsonSchema={frontmatter.customField.jsonSchema}
  typeSpec={frontmatter.customField.typeSpec}
  python={frontmatter.customField.python}
/>

## CustomFieldType

The set of JSON schema types supported by a custom field.

### Options

| Value   | Description             |
| ------- | ----------------------- |
| string  | Text values             |
| integer | Whole numbers           |
| number  | Numeric values          |
| boolean | True/false values       |
| object  | Structured data objects |
| array   | Lists of values         |

### Formats

<SchemaFormatTabs
  example={frontmatter.customFieldType.example}
  jsonSchema={frontmatter.customFieldType.jsonSchema}
  typeSpec={frontmatter.customFieldType.typeSpec}
  python={frontmatter.customFieldType.python}
/>
