---
title: PCS term
description: A Philanthropy Classification System (PCS) term.
pcsTerm:
  example:
    code: |
      {
        "term": "Nonprofit",
        "class": "Organization types",
        "code": "UC000000",
        "description": "Nonprofit organization"
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/PCSTerm.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/pcs.tsp"
      startLine: 15
      endLine: 30
pcsClass:
  example:
    code: '"Organization types"'
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/PCSClass.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/pcs.tsp"
      startLine: 36
      endLine: 43
pcsOrgType:
  example:
    code: |
      {
        "term": "Nonprofit",
        "class": "Organization types",
        "code": "UC000000",
        "description": "Nonprofit organization"
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/PCSOrgType.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/pcs.tsp"
      startLine: 49
      endLine: 56
pcsSubject:
  example:
    code: |
      {
        "term": "Education",
        "class": "Subjects",
        "code": "SB000000",
        "description": "All formally constituted educational institutions (except art and performing art schools) and projects or activities."
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/PCSSubject.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/pcs.tsp"
      startLine: 58
      endLine: 65
pcsPopulation:
  example:
    code: |
      {
        "term": "Children and youth",
        "class": "Population groups",
        "code": "PA010000",
        "description": "Young people from birth to 18 years of age."
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/PCSPopulation.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/pcs.tsp"
      startLine: 67
      endLine: 74
pcsSupportStrategy:
  example:
    code: |
      {
        "term": "Cash grants",
        "class": "Support strategies",
        "code": "TA000000",
        "description": "Direct monetary contributions of cash or cash equivalents."
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/PCSSupportStrategy.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/pcs.tsp"
      startLine: 76
      endLine: 83
pcsTransactionType:
  example:
    code: |
      {
        "term": "Cash grants",
        "class": "Transaction types",
        "code": "TA000000",
        "description": "Direct monetary contributions of cash or cash equivalents."
      }
  jsonSchema:
    file:
      path: "website/public/schemas/yaml/PCSTransactionType.yaml"
  typeSpec:
    file:
      path: "lib/core/lib/core/fields/pcs.tsp"
      startLine: 85
      endLine: 92
---

import SchemaFormatTabs from "@/components/SchemaFormatTabs.astro";

## PCSTerm

A term in the Philanthropy Classification System (PCS).

Visit [taxonomy.candid.org](https://taxonomy.candid.org/) for more information.

### Table

| Property       | Type   | Required | Description                                |
| -------------- | ------ | -------- | ------------------------------------------ |
| term           | string | Yes      | The term for the organization type.        |
| Classification | string | Yes      | The class for the organization type.       |
| code           | string | Yes      | The code for the organization type.        |
| description    | string | No       | The description for the organization type. |

### Formats

<SchemaFormatTabs
  example={frontmatter.pcsTerm.example}
  jsonSchema={frontmatter.pcsTerm.jsonSchema}
  typeSpec={frontmatter.pcsTerm.typeSpec}
/>

## PCSClass

The class to which the PCS term belongs.

### Table

| Value             | Description                       |
| ----------------- | --------------------------------- |
| orgTypes          | The class for organization types. |
| subjects          | The class for subjects.           |
| populationGroups  | The class for population groups.  |
| transactionTypes  | The class for transaction types.  |
| supportStrategies | The class for support strategies. |

### Formats

<SchemaFormatTabs
  example={frontmatter.pcsClass.example}
  jsonSchema={frontmatter.pcsClass.jsonSchema}
  typeSpec={frontmatter.pcsClass.typeSpec}
/>

## Sub-types

PCS terms are divided into the following sub-types based on the `class` property:

- [PCSOrgType](#pcsorgtype)
- [PCSSubject](#pcssubject)
- [PCSPopulation](#pcspopulation)
- [PCSSupportStrategy](#pcssupportstrategy)
- [PCSTransactionType](#pcstransactiontype)

### PCSOrgType

A [PCSTerm](#pcsterm) where the `class` is `"Organization types"`. Categorizes the type of organization receiving philanthropic support.

See [PCS organization types](https://taxonomy.candid.org/organization-type) for more information.

<SchemaFormatTabs
  example={frontmatter.pcsOrgType.example}
  jsonSchema={frontmatter.pcsOrgType.jsonSchema}
  typeSpec={frontmatter.pcsOrgType.typeSpec}
/>

### PCSSubject

A [PCSTerm](#pcsterm) where the `class` is `"Subjects"`. Categorizes the subject or focus of philanthropic activities.

See [PCS subjects](https://taxonomy.candid.org/subject) for more information.

<SchemaFormatTabs
  example={frontmatter.pcsSubject.example}
  jsonSchema={frontmatter.pcsSubject.jsonSchema}
  typeSpec={frontmatter.pcsSubject.typeSpec}
/>

### PCSPopulation

A [PCSTerm](#pcsterm) where the `class` is `"Population groups"`. Categorizes the population being served by philanthropic activities.

See [PCS population groups](https://taxonomy.candid.org/population-group) for more information.

<SchemaFormatTabs
  example={frontmatter.pcsPopulation.example}
  jsonSchema={frontmatter.pcsPopulation.jsonSchema}
  typeSpec={frontmatter.pcsPopulation.typeSpec}
/>

### PCSSupportStrategy

A [PCSTerm](#pcsterm) where the `class` is `"Support strategies"`. Categorizes the strategy used to support philanthropic activities.

See [PCS support strategies](https://taxonomy.candid.org/support-strategy) for more information.

<SchemaFormatTabs
  example={frontmatter.pcsSupportStrategy.example}
  jsonSchema={frontmatter.pcsSupportStrategy.jsonSchema}
  typeSpec={frontmatter.pcsSupportStrategy.typeSpec}
/>

### PCSTransactionType

A [PCSTerm](#pcsterm) where the `class` is `"Transaction types"`. Categorizes the type of transaction used to support philanthropic activities. For example, "Grant" or "Loan".

See [PCS transaction types](https://taxonomy.candid.org/transaction-type) for more information.

<SchemaFormatTabs
  example={frontmatter.pcsTransactionType.example}
  jsonSchema={frontmatter.pcsTransactionType.jsonSchema}
  typeSpec={frontmatter.pcsTransactionType.typeSpec}
/>
